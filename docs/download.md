# Download and Extract Datasets

To download and extract datasets for different audit log formats, use the provided scripts in the `src/download_scripts` directory.

Currently, ProvPlug supports downloading datasets for the following:
- DARPA E3 Dataset
- OPTC Dataset
